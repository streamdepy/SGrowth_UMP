<main class="flex flex-col ml-48 self-center mb-16 mt-16 w-full max-w-[1256px] max-md:mt-10 max-md:max-w-full">
  <header class="flex gap-3.5 self-start">
    <img
      src="https://api.builder.io/api/v1/image/assets/TEMP/c44af07e3c930c7fea223a8eb981ce2b2a5a5a8c?width=462"
      alt="GRI Environment icon"
      class="object-contain shrink-0 my-auto rounded-full aspect-[1.04] w-[78px]"
    />
    <div class="flex flex-col">
      <h2 class="text-4xl font-semibold text-black">GRI Governance</h2>
      <p class="text-xl font-medium leading-none text-gray-500">
        Governance Performance
      </p>
    </div>
  </header>

<!-- GRI 205-3 — Proven Corruption Incidents & Actions Taken -->
<section class="flex flex-col items-start py-14 px-16 mt-10 w-full bg-white rounded-2xl shadow-2xl max-w-[981px] max-md:px-5">
  <h3 class="text-3xl font-semibold text-teal-700">
    GRI 205-3 — Proven Corruption Incidents & Actions Taken
  </h3>
  <p class="mt-3 text-gray-500">
    Fill in the information regarding proven corruption incidents and the actions taken by the company.
  </p>

  <form id="gri-205-3-form" class="w-full mt-8 space-y-8">
    <!-- 1. Total number of proven corruption incidents -->
    <div class="space-y-3">
      <div class="flex items-center justify-between gap-2">
        <label class="block font-medium text-neutral-900">
          Total number of proven corruption incidents
        </label>
        <span class="text-xs text-gray-400">
          GRI 205-3(a)
        </span>
      </div>
      <p class="text-xs text-gray-500">
        Including violations related to bribery, gratuities, conflicts of interest, and other forms of corruption.
      </p>

      <div class="grid grid-cols-2 gap-5 max-md:grid-cols-1">
        <div>
          <label class="block pb-2 font-medium text-neutral-900">
            Total proven corruption incidents
          </label>
          <input
            type="number"
            min="0"
            placeholder="e.g., 0"
            class="w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border-gray-300 border border-solid bg-slate-50 placeholder-black/50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition"
          />
        </div>
        <div>
          <label class="block pb-2 font-medium text-neutral-900">
            Brief summary (optional)
          </label>
          <textarea
            rows="3"
            placeholder="e.g., 1 bribery case, 1 conflict of interest"
            class="w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border-gray-300 border border-solid bg-slate-50 placeholder-black/50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition resize-y"
          ></textarea>
        </div>
      </div>
    </div>

    <!-- 2. Incidents leading to termination / sanctions -->
    <div class="space-y-3">
      <div class="flex items-center justify-between gap-2">
        <label class="block font-medium text-neutral-900">
          Incidents leading to termination / sanctions against employees
        </label>
        <span class="text-xs text-gray-400">
          GRI 205-3(b)
        </span>
      </div>
      <p class="text-xs text-gray-500">
        Data and internal follow-up on violations (termination, suspension, disciplinary sanctions).
      </p>

      <div class="grid grid-cols-2 gap-5 max-md:grid-cols-1">
        <div>
          <label class="block pb-2 font-medium text-neutral-900">
            Number of incidents resulting in termination / sanctions
          </label>
          <input
            type="number"
            min="0"
            placeholder="e.g., 0"
            class="w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border-gray-300 border border-solid bg-slate-50 placeholder-black/50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition"
          />
        </div>
        <div>
          <label class="block pb-2 font-medium text-neutral-900">
            Details of internal follow-up
          </label>
          <textarea
            rows="3"
            placeholder="e.g., 1 employee terminated, 2 employees given mild disciplinary sanctions."
            class="w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border-gray-300 border border-solid bg-slate-50 placeholder-black/50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition resize-y"
          ></textarea>
        </div>
      </div>
    </div>

    <!-- 3. Incidents leading to contract termination with business partners -->
    <div class="space-y-3">
      <div class="flex items-center justify-between gap-2">
        <label class="block font-medium text-neutral-900">
          Incidents leading to contract termination with business partners
        </label>
        <span class="text-xs text-gray-400">
          GRI 205-3(c)
        </span>
      </div>
      <p class="text-xs text-gray-500">
        Including vendors, suppliers, contractors, or other third parties whose contracts were terminated due to corruption.
      </p>

      <div class="grid grid-cols-2 gap-5 max-md:grid-cols-1">
        <div>
          <label class="block pb-2 font-medium text-neutral-900">
            Number of business partners whose contracts were terminated
          </label>
          <input
            type="number"
            min="0"
            placeholder="e.g., 0"
            class="w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border-gray-300 border border-solid bg-slate-50 placeholder-black/50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition"
          />
        </div>
        <div>
          <label class="block pb-2 font-medium text-neutral-900">
            Details of partners / type of partnership (optional)
          </label>
          <textarea
            rows="3"
            placeholder="e.g., 1 raw materials supplier, 1 construction project contractor."
            class="w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border-gray-300 border border-solid bg-slate-50 placeholder-black/50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition resize-y"
          ></textarea>
        </div>
      </div>
    </div>

    <!-- 4. Public legal cases related to corruption -->
    <div class="space-y-3">
      <div class="flex items-center justify-between gap-2">
        <label class="block font-medium text-neutral-900">
          Public legal cases related to corruption
        </label>
        <span class="text-xs text-gray-400">
          GRI 205-3(d)
        </span>
      </div>
      <p class="text-xs text-gray-500">
        Number and status of legal cases involving the company or employees (court cases, investigations, etc.).
      </p>

      <div class="grid grid-cols-2 gap-5 max-md:grid-cols-1">
        <div>
          <label class="block pb-2 font-medium text-neutral-900">
            Number of public legal cases related to corruption
          </label>
          <input
            type="number"
            min="0"
            placeholder="e.g., 0"
            class="w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border-gray-300 border border-solid bg-slate-50 placeholder-black/50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition"
          />
        </div>
        <div>
          <label class="block pb-2 font-medium text-neutral-900">
            Case status & summary
          </label>
          <textarea
            rows="3"
            placeholder="e.g., 1 case under investigation, 1 case decided in court."
            class="w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border-gray-300 border border-solid bg-slate-50 placeholder-black/50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition resize-y"
          ></textarea>
        </div>
      </div>
    </div>

    <!-- 5. Remediation actions -->
    <div class="space-y-3">
      <div class="flex items-center justify-between gap-2">
        <label class="block font-medium text-neutral-900">
          Remediation actions
        </label>
        <span class="text-xs text-gray-400">
          IFRS S1 §26
        </span>
      </div>
      <p class="text-xs text-gray-500">
        Describe new policies, strengthened systems, enhanced monitoring, or improvements made.
      </p>

      <textarea
        rows="4"
        placeholder="e.g., revision of anti-bribery policy, improvement of whistleblowing system, additional audit in high-risk areas, further training for management."
        class="w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border-gray-300 border border-solid bg-slate-50 placeholder-black/50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition resize-y"
      ></textarea>
    </div>

    <!-- Navigation Buttons (customize route accordingly) -->
    <div class="flex justify-end gap-4 mt-8 w-full">
        <button
          type="button"
          onclick="window.history.back()"
          class="px-8 py-2 text-base font-medium text-center text-teal-700 border-teal-700 border border-solid whitespace-nowrap bg-white rounded-xl hover:bg-teal-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2 transition-colors"
        >
          Previous
        </button>
      <button
        type="submit"
        class="px-8 py-2 text-base font-medium text-center text-white bg-lime-500 rounded-xl hover:bg-lime-600 transition"
      >
        Save
      </button>
    </div>
  </form>
</section>


</main>

<script>
  // Tampilkan "Frekuensi evaluasi" hanya jika jawabannya "Ya"
  document.addEventListener('DOMContentLoaded', () => {
    const evalSel = document.getElementById('pol_eval');
    const freqWrap = document.getElementById('pol_eval_freq_wrap');

    function syncEval() {
      const show = evalSel && evalSel.value === 'ya';
      if (!freqWrap) return;
      freqWrap.classList.toggle('hidden', !show);
      const freqSel = document.getElementById('pol_eval_freq');
      if (freqSel) freqSel.disabled = !show;
      if (!show && freqSel) freqSel.value = '';
    }

    if (evalSel) {
      evalSel.addEventListener('change', syncEval);
      syncEval();
    }
  });
</script>
