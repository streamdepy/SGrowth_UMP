<main class="flex flex-col ml-48 self-center mb-16 mt-16 w-full max-w-[1256px] max-md:mt-10 max-md:max-w-full">
  <header class="flex gap-3.5 self-start">
    <img
      src="https://api.builder.io/api/v1/image/assets/TEMP/1c71a2a9be3de365531c3991c3339c7e3c083b53?width=462"
      alt="GRI Environment icon"
      class="object-contain shrink-0 my-auto rounded-full aspect-[1.04] w-[58px]"
    />
    <div class="flex flex-col">
      <h2 class="text-4xl font-semibold text-black">GRI Environment</h2>
      <p class="self-center text-xl font-medium leading-none text-center text-gray-500">
        Environmental Performance
      </p>
    </div>
  </header>

<!-- Scope 3 (Tidak Langsung Lainnya) -->
<section class="flex flex-col items-start py-14 px-16 mt-10 w-full bg-white rounded-2xl shadow-2xl max-w-[981px] max-md:px-5">
  <h3 class="text-3xl font-semibold text-teal-700">Emisi Scope 3 (Tidak Langsung Lainnya)</h3>
  <p class="mt-3 text-gray-500">Isi data emisi tidak langsung lainnya (Scope 3).</p>

  <!-- Q1: Menghitung Scope 3? -->
  <div class="w-full mt-8">
    <label for="scope3_has" class="block pb-2 text-base font-medium">Apakah perusahaan menghitung emisi Scope 3?</label>
    <select id="scope3_has" name="scope3_has"
      class="w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border border-gray-300 bg-slate-50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition">
      <option value="tidak">Tidak</option>
      <option value="ya">Ya</option>
      
    </select>
  </div>

  <!-- Detail Scope 3 (tampil hanya jika Ya) -->
  <div id="scope3_details" class="w-full mt-6 hidden">
    <!-- Daftar entri Scope 3 -->
    <div id="scope3_list" class="space-y-8">
      <!-- Item #0 (awalan) -->
      <div class="s3-item border border-gray-200 rounded-2xl p-4">
        <!-- Total emisi Scope 3 -->
        <div class="w-full">
          <label class="block pb-2 text-base font-medium">Total emisi Scope 3 (tCO₂e)</label>
          <input type="number" min="0" step="0.0001" name="s3[0][total]" placeholder="0"
            class="s3-total w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border border-gray-300 bg-slate-50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition">
        </div>

        <!-- Sumber utama Scope 3 -->
        <div class="w-full mt-6">
          <label class="block pb-2 text-base font-medium">Sumber utama emisi Scope 3</label>
          <select name="s3[0][source]" class="s3-source w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border border-gray-300 bg-slate-50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition">
            <option value="">Pilih</option>
            <option>Rantai Pasok</option>
            <option>Transportasi Pihak Ketiga</option>
            <option>Perjalanan Dinas</option>
            <option>Limbah</option>
            <option value="lainnya">Lainnya</option>
          </select>
          <input type="text" name="s3[0][source_other]" placeholder="Sebutkan sumber lainnya"
            class="s3-source-other w-full px-4 py-3 mt-3 rounded-lg border border-gray-300 bg-slate-50 hidden">
        </div>

        <!-- Tombol hapus entri -->
        <div class="mt-4 flex justify-end">
          <button type="button" class="s3-remove hidden px-4 py-2 rounded-xl border border-gray-300 text-gray-700 hover:bg-gray-50 transition">
            Hapus entri
          </button>
        </div>
      </div>
    </div>

    <!-- Tombol Tambah item -->
    <div class="mt-6 flex justify-end">
      <button type="button" id="scope3_add_item"
        class="inline-flex items-center gap-2 px-4 py-2 rounded-xl text-teal-700 text-sm font-medium transition">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none"
             viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4"/>
        </svg>
        Tambah
      </button>
    </div>
  </div>
      <!-- Buttons pojok kanan kotak putih -->
  <div class="flex justify-end gap-4 mt-10 w-full">
    <button type="button" onclick="window.history.back()"
      class="px-8 py-2 text-base font-medium text-center text-teal-700 border-teal-700 border border-solid whitespace-nowrap bg-white rounded-xl hover:bg-teal-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2 transition-colors">
      Sebelumnya
    </button>
    <a href="/umkm/env-5"
      class="px-8 py-2 text-base font-medium text-center text-white whitespace-nowrap bg-lime-500 rounded-xl hover:bg-lime-600 focus:outline-none focus:ring-2 focus:ring-lime-500 focus:ring-offset-2 transition-colors">
      Simpan
    </a>
  </div>
</section>

<!-- Template item Scope 3 -->
<template id="scope3_item_tpl">
  <div class="s3-item border border-gray-200 rounded-2xl p-4">
    <div class="w-full">
      <label class="block pb-2 text-base font-medium">Total emisi Scope 3 (tCO₂e)</label>
      <input type="number" min="0" step="0.0001" name=""
        placeholder="0"
        class="s3-total w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border border-gray-300 bg-slate-50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition">
    </div>

    <div class="w-full mt-6">
      <label class="block pb-2 text-base font-medium">Sumber utama emisi Scope 3</label>
      <select name="" class="s3-source w-full px-8 py-4 mt-2 text-base text-black rounded-2xl border border-gray-300 bg-slate-50 outline-none hover:ring focus:ring-2 focus:ring-teal-600 transition">
        <option value="">Pilih</option>
        <option>Rantai Pasok</option>
        <option>Transportasi Pihak Ketiga</option>
        <option>Perjalanan Dinas</option>
        <option>Limbah</option>
        <option value="lainnya">Lainnya</option>
      </select>
      <input type="text" name="" placeholder="Sebutkan sumber lainnya"
        class="s3-source-other w-full px-4 py-3 mt-3 rounded-lg border border-gray-300 bg-slate-50 hidden">
    </div>

    <div class="mt-4 flex justify-end">
      <button type="button" class="s3-remove px-4 py-2 rounded-xl border border-gray-300 text-gray-700 hover:bg-gray-50 transition">
        Hapus entri
      </button>
    </div>
  </div>
</template>



</main>
<script>
  // Jika helper toggleEl belum ada, gunakan ini:
  function toggleEl(el, show, disable = true) {
    if (!el) return;
    el.classList.toggle('hidden', !show);
    if (disable) el.disabled = !show;
    if (!show && (el.tagName === 'INPUT' || el.tagName === 'TEXTAREA')) el.value = '';
    if (!show && el.tagName === 'SELECT') el.value = '';
  }

  document.addEventListener('DOMContentLoaded', function () {
    const hasSel  = document.getElementById('scope3_has');
    const details = document.getElementById('scope3_details');
    const list    = document.getElementById('scope3_list');
    const addBtn  = document.getElementById('scope3_add_item');
    const tpl     = document.getElementById('scope3_item_tpl');

    function syncHas() {
      const yes = hasSel && hasSel.value === 'ya';
      toggleEl(details, yes, false);
      if (details) {
        details.querySelectorAll('input, select, textarea, button').forEach(el => {
          if (el.id === 'scope3_add_item') { el.disabled = !yes; return; }
          el.disabled = !yes;
        });
      }
    }

    if (hasSel) {
      hasSel.addEventListener('change', syncHas);
      syncHas();
    }

    if (!list || !addBtn || !tpl) return;

    function reindex() {
      const items = list.querySelectorAll('.s3-item');
      items.forEach((item, idx) => {
        item.querySelectorAll('input, select, textarea').forEach(el => {
          if (el.classList.contains('s3-total'))        el.name = `s3[${idx}][total]`;
          else if (el.classList.contains('s3-source'))  el.name = `s3[${idx}][source]`;
          else if (el.classList.contains('s3-source-other')) el.name = `s3[${idx}][source_other]`;
        });

        const rm = item.querySelector('.s3-remove');
        if (rm) rm.classList.toggle('hidden', items.length <= 1);
      });
    }

    function attachLogic(item) {
      const sourceSel = item.querySelector('.s3-source');
      const sourceOther = item.querySelector('.s3-source-other');

      function syncSource() {
        const isOther = sourceSel && sourceSel.value === 'lainnya';
        toggleEl(sourceOther, isOther);
      }

      if (sourceSel) { sourceSel.addEventListener('change', syncSource); syncSource(); }

      const rmBtn = item.querySelector('.s3-remove');
      if (rmBtn) {
        rmBtn.addEventListener('click', () => {
          const items = list.querySelectorAll('.s3-item');
          if (items.length > 1) {
            item.remove();
            reindex();
          }
        });
      }
    }

    // item awal
    const first = list.querySelector('.s3-item');
    if (first) attachLogic(first);
    reindex();

    // tombol tambah
    addBtn.addEventListener('click', () => {
      const clone = tpl.content.firstElementChild.cloneNode(true);
      list.appendChild(clone);
      attachLogic(clone);
      reindex();
    });
  });
</script>
